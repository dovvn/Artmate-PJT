<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.artmate.mapper.FileMapper">
	<insert id="saveFile" parameterType="MyFileDto">
		INSERT INTO FILE_TB(origin) VALUES(#{origin})
	</insert>
	
	<select id="searchFile" resultType="MyFileDto">
		SELECT * FROM FILE_TB WHERE fno=#{fno};
	</select>
</mapper>